def sum (a,b)
{
  echo "the sum of ${a} and ${b} of ${a+b}"
}
def sub (a,b)
{
  echo "the subtraction of ${a} and ${b} of ${a-b}"
}
def mul (a,b)
{
  echo "the multipal of ${a} and ${b} of ${a*b}"
}
def dev (a,b)
{
  echo "the devision of ${a} and ${b} of ${a/b}"
}
